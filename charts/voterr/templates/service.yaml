apiVersion: v1
kind: Service
metadata:
  name: {{ include "voterr.fullname" . }}
  labels:
    {{- include "voterr.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "voterr.selectorLabels" . | nindent 4 }}
